export const cartError = (cid) => {
    return `El carrito con id: ${cid} no se encuentra en la base de datos`;
}

export const productError = (pid) => {
    return `El producto con id: ${pid} no se encuentra en la base de datos`;
}

