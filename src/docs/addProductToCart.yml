paths:
  /api/carts/:cid/product/:pid:
    post:
      summary: Add product to cart by id
      tags:
        - Carrito
      responses:
        "201":
          description: Cart.
          content:
            application/json:
              schema:
                type: array
        "400":
          description: Forbidden Request        